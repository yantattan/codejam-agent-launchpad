# Browser/server
HOST=0.0.0.0
PORT=3000
PUBLIC_PORT=3000
LOG_LEVEL=info

# Volcengine Ark. ARK_MODEL is an endpoint/model ID that supports the
# OpenAI-compatible Responses API, for example ep-xxxxxxxx.
ARK_API_KEY=replace-with-your-ark-api-key
ARK_MODEL=ep-replace-with-your-endpoint-id
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# Supabase Auth. Pre-filled with a project set up for reviewers to sign in
# against out of the box — no setup required to try the app. The URL and
# "publishable" (anon) key below are Supabase's public client credentials,
# explicitly designed to be shipped in client code and safe to commit: they
# only grant what Row Level Security allows, never raw database access. To
# use your own Supabase project instead, replace all four with the values
# from your own project at Settings > API. The server uses the SUPABASE_*
# pair to verify who is calling the API; the browser uses the VITE_-prefixed
# copies to run the sign-in/sign-up flow.
SUPABASE_URL=https://fqazxonyjwgopqiwtagq.supabase.co
SUPABASE_ANON_KEY=sb_publishable_JxVoXb2Cq_98YnlOSgi9og_jpiQzA1a
VITE_SUPABASE_URL=https://fqazxonyjwgopqiwtagq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_JxVoXb2Cq_98YnlOSgi9og_jpiQzA1a

# Optional, genuinely secret — leave empty. Persists Agents/Messages/Runs in
# Postgres instead of the local .data/launchpad.json file, so they follow
# your account across machines. This needs your OWN Supabase project (not
# the shared one above, since its service_role key would expose every
# reviewer's data to every other reviewer): run
# docs/supabase-agents-schema.sql once in your project's SQL Editor, then
# set this to that project's service_role key (Settings > API — the SECRET
# key, never the anon/publishable key). Leave empty to keep everything
# local-only. NEVER add a VITE_ copy of this — it must never reach the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Runtime paths (Docker defaults are already correct).
APP_DATA_DIR=/app/data
AGENT_WORKSPACE_ROOT=/app/workspaces
CODEX_HOME=/app/codex-home
CODEX_BIN=codex

# Codex runs inside the dedicated ECS/container boundary. Startup keeps
# workspace-write when Landlock is supported and falls back to
# danger-full-access inside the outer container on kernels without Landlock.
# The fallback has no per-Agent filesystem isolation; use a scoped demo model
# key and do not place unrelated credentials or data in this POC.
CODEX_SANDBOX_MODE=workspace-write
CODEX_TIMEOUT_MS=600000
CODEX_MAX_OUTPUT_BYTES=2097152

# Runtime provider. The ECS/Compose image uses local-process because Codex is
# installed in the application container. scripts/start-local-poc.sh overrides
# this to container and starts a disposable Docker/Colima/Podman Runtime.
RUNTIME_PROVIDER=local-process
CONTAINER_ENGINE=docker
CONTAINER_RUNTIME_IMAGE=volc-agent-runtime:local
# Optional build mirrors for networks that cannot reach Docker Hub or
# deb.debian.org. Leave empty on normal networks.
CONTAINER_RUNTIME_BASE_IMAGE=node:22-bookworm-slim
CONTAINER_APT_MIRROR=
CONTAINER_APT_SECURITY_MIRROR=
# Optional extra tools, for example append: python3 build-essential
CONTAINER_RUNTIME_APT_PACKAGES=ca-certificates git ripgrep
CONTAINER_CPU_LIMIT=2
CONTAINER_MEMORY_LIMIT=2g
CONTAINER_PIDS_LIMIT=256
RUNTIME_INSTANCE_ID=default
# CONTAINER_USER is filled with the host UID:GID by start-local-poc.sh.
# CONTAINER_USER=1000:1000
